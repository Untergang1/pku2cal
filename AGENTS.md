# 开发约定

## 项目方向

- 将北京大学个人课表转换为可订阅的 ICS；实现进度与验证边界见 README 和 docs/verification.md。
- 课表仅通过显式命令手动拉取，保存为 data 中可编辑的 YAML；生成与发布以本地文件为唯一来源，不自动登录或刷新。
- 同时支持 GitHub Actions 手动发布 Pages 快照和 Cloudflare Worker 提供部署时嵌入的 ICS；Worker 通过 Wrangler CLI 部署，不使用 KV。两种发布方式共用本地生成逻辑，部署行为留在各自入口中。
- 保持认证、页面获取与解析、日历生成、发布或 HTTP 响应的职责清晰，避免上游页面变化影响日历逻辑。不预设语言或框架；选型需兼顾两种运行环境。
- 使用同一套代码和工具流程支持 macOS 与 Linux 的本地开发、测试、ICS 生成和部署。依赖需支持两种系统，路径使用跨平台 API，脚本避免依赖单个平台的命令或 GNU/BSD 特有参数；必要的平台差异集中封装。

## 参考资料与日历行为

- 开发课表获取功能前阅读 [参考资料](docs/reference.md)。认证和选课页面优先参考 pkucli，北大课程时间语义参考 PekingParser；外部实现的接口和页面假设仍需核实。
- 复用外部代码前核查许可证及署名要求。Sleepy 的 ICS 导出仅作行为参考，避免引入未经确认的 GPL 代码。
- 课程时间按 `Asia/Shanghai` 解释，正确处理教学周、单双周和同一课程的多个时段；同一事件在重新生成后保持稳定 UID，避免订阅重复。

## 数据与安全

- 不提交登录凭据、会话、个人课表或生成的个人 ICS；不在日志中输出这些敏感信息。
- 北大凭据只通过本地未跟踪环境配置提供，供显式拉取使用；不上传至发布平台。订阅令牌及 Pages 发布快照通过平台 Secrets 提供，不写入代码或公开配置。
- Pages 发布的 ICS 应按公开可访问资源处理；静态产物通过部署流程发布，不为发布而将个人 ICS 加入源码版本控制。

## 验证要求

- 按变更范围验证；功能实现需覆盖课程时间解析、ICS 格式与稳定 UID，以及两种入口的一致性。文档变更检查内容和相对链接即可。
- 引入可运行实现时，配置 macOS 与 Linux 的 CI 测试矩阵，使用相同命令验证依赖安装、构建和测试；涉及脚本或部署工具的变更需在两种系统上验证，部署检查使用不发布的构建或 dry-run。
