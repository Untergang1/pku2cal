{
  "name": "pku2cal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "check": "npm run typecheck && npm run build && npm test",
    "probe": "npm run build && node --env-file-if-exists=.env scripts/probe.mjs",
    "probe:worker": "node --env-file-if-exists=.env scripts/probe-worker.mjs",
    "generate": "npm run build && node --env-file-if-exists=.env dist/entrypoints/node.js",
    "setup": "npm run build && node dist/entrypoints/setup.js",
    "status": "npm run build && node dist/entrypoints/node.js --status",
    "worker:check": "node scripts/worker-check.mjs",
    "worker:build": "npm run build && node scripts/build-worker.mjs",
    "worker:dev": "wrangler dev",
    "worker:deploy": "wrangler deploy",
    "token": "node -e \"console.log(require('node:crypto').randomBytes(32).toString('base64url'))\""
  },
  "dependencies": {
    "cheerio": "1.2.0",
    "tough-cookie": "6.0.2",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@types/node": "22.20.4",
    "esbuild": "0.28.2",
    "ical.js": "2.2.1",
    "miniflare": "5.20260921.0-alpha",
    "typescript": "7.0.2",
    "vitest": "5.0.1",
    "wrangler": "4.137.0"
  }
}
